# Copy to .env.production and fill in before running `npm run build` for
# the AWS deployment - Vite automatically uses .env.production for builds.
# Until a domain exists (deferred to deployment day), use the EC2 instance's
# public IP, e.g. http://<ec2-public-ip>:5000
VITE_API_URL=https://api.yourdomain.com
